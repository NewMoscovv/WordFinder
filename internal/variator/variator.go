package variator

import (
	"strings"
)

func SetFileType(fileType string) {
	fileType = strings.ToLower(fileType)

	switch fileType {
	case "excel":
		// here is not logic.
		// -- pls implement me!
	}
}
